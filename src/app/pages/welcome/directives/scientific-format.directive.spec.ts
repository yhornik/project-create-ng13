import { ExponentialFormatDirective } from './scientific-format.directive';

describe('ExponentialFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new ExponentialFormatDirective();
    expect(directive).toBeTruthy();
  });
});
