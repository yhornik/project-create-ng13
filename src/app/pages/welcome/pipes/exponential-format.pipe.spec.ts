import { ExponentialFormatPipe } from './exponential-format.pipe';

describe('ExponentialFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ExponentialFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
